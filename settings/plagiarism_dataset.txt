for (int i = 0; i < n; ++i) { sum += arr[i]; }
std::vector<int> nums; nums.push_back(1); nums.push_back(2);
while (left < right) { std::swap(v[left], v[right]); left++; right--; }
if (node == nullptr) { return; }
template <typename T> class Matrix { public: T at(int r, int c) const; };